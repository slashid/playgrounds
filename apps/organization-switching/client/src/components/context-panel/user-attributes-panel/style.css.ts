import { style } from "@vanilla-extract/css";

export const panel = style({
  marginBottom: "12px",
});

export const attributesPanel = style({
  marginBottom: "24px",
  marginTop: "24px",
});

export const content = style({
  display: "grid",
  gap: "10px",
});

export const header = style({
  paddingBottom: "2px",
});
