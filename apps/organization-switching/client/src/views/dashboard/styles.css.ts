import { style } from "@vanilla-extract/css";

export const admin = style({
  marginTop: "40px",
});

export const createOrgButton = style({
  maxWidth: "235px",
  justifySelf: "flex-end",
  marginLeft: "auto",
});
