import { style } from "@vanilla-extract/css";

export const container = style({
  display: "grid",
  placeItems: "center",
  width: "100%",
  height: "100%",
});

export const formWrapper = style({
  width: "465px",
});
