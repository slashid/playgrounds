import { style } from "@vanilla-extract/css";

export const imageWrapper = style({
  display: "grid",
  width: "100%",
  aspectRatio: "1 / 1",
});

export const image = style({
  width: "100%",
  aspectRatio: "1 / 1",
});
