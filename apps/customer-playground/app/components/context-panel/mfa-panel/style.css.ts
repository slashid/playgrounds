import { style } from "@vanilla-extract/css";

export const panel = style({
  marginBottom: "12px",
});

export const mfaCtaPanel = style({
  display: "grid",
  gap: "28px",
});

export const formContainer = style({
  display: "grid",
  gap: "10px",
});
